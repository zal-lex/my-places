
<header>
  <style>
    table {
      font-family: arial, sans-serif;
      border-collapse: collapse;
      width: 100%;
    }

    td, th {
      border: 1px solid #dddddd;
      text-align: left;
      padding: 8px;
    }

    tr:nth-child(even) {
      background-color: #dddddd;
    }
    table#t01 {
      width: 100%;    
      background-color: #e86881;
    }
  </style>
  <div >
    <h1 style="text-align:center;">Statistics for Favorite Places</h1>
  </div>
</header>
<p>&nbsp;</p>
<h2 style="text-align:center;">General statistics on <%= Time.now.to_formatted_s(:short) %> </h2>
<table id="t01">
  <thead>
    <tr>
      <th>New users today</th>
      <th>New places today</th>
      <th>New liked places today</th>
    </tr>
  </thead>
  <tbody>
    <tr>
    <td><%= user.where("DATE(created_at) = DATE(?)", Time.now).count %></td>
    <td><%= place.where("DATE(created_at) = DATE(?)", Time.now).count %></td>
    <td><%= fav_place.where("DATE(created_at) = DATE(?)", Time.now).count %></td>
    </tr>
  </tbody>
</table>
<p>&nbsp;</p>
<h2 style="text-align:center;">Detailed statistics for all users on <%= Time.now.to_formatted_s(:short) %> </h2>
<table class="table">
   <thead>
    <tr>
      <th class="quantity">Username</th>
      <th class="item-number">Total places</th>
      <th class="description">Total friends</th>
      <th class="unit-price">Total likes</th>
    </tr>
  </thead>
  <tbody>
    <% user.each do |user| %> 
      <tr>
      <td><%= user.username%></td>
      <td><%= user.places.count %></td>
      <td><%= user.active_friendships.count %></td>
      <td><%= user.fav_places.count %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<p>&nbsp;</p>
<p>&nbsp;</p>
<p>&nbsp;</p>
<div>
  <h3 style="text-align:center;">Thanks for visiting our page!</h3>
</div>